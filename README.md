# Destination Change Streamlit App

Run locally:

```bash
pip install -r requirements.txt
streamlit run destination_change_streamlit_app.py
```

Optional OSQP second-pass sheets can be enabled in the app. The main Optimized Data sheet is preserved and includes sub/other vendor destination-change suggestion columns.
